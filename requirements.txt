# Tested on Python 3.11-3.13 (CPython). NOTE: Python 3.14 is NOT yet supported by
# the pinned pandas (2.3.1) -- its datetime C-extension segfaults on 3.14. Use a
# Python 3.11-3.13 interpreter (e.g. `python3.13 -m venv .venv`) to reproduce.

# Core scientific computing
numpy==2.3.4
pandas==2.3.1
scipy==1.16.2

# Statistical modeling
# (granger/VAR, multipletests, proportions_ztest, acorr_ljungbox, adfuller/kpss — all stable since 0.14.x)
statsmodels>=0.14.1
# Change-point detection (PELT, Bai-Perron-style trimming) — used by code/c3_bai_perron.py
ruptures>=1.1.7

# GARCH models (optional - manual implementation provided)
# arch==6.4.0  # Uncomment if using arch package models instead of manual TARCH-X

# Machine learning
scikit-learn==1.7.2

# Visualization
matplotlib==3.10.6
seaborn==0.13.2

# Data handling
python-dateutil==2.9.0
pytz==2025.2

# HTTP requests (for CoinGecko API)
requests==2.32.5
urllib3==2.5.0

# Parquet engine — only needed to (re)build and read the Binance daily-kline
# cache behind the c11 returns-leg smoke test (code/fetch_binance_cache.py).
# The c11 gate runs and every other script work without it.
pyarrow>=15.0

# Optional utilities
colorama==0.4.6
termcolor==3.1.0
joblib==1.4.2

# Development/testing dependencies
# See requirements-test.txt for testing requirements
